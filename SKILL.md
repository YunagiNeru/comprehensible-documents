---
name: comprehensible-documents
description: 人間とAIが同じ正本として直接読める自己完結HTML文書を新規作成、改訂、監査する。要件定義、概要設計、詳細設計、ADR、ランブック、運用、セキュリティ、API参照、調査、解説などの技術文書をHTMLで作るとき、既存HTML文書の構造や可読性を統一するとき、または理解負債を監査するときに使用する。通常の「@〇〇.htmlを読んで実装する」という参照だけでは使用しない。WebアプリやランディングページのUI設計には使用しない。
---

# Comprehensible Documents

## 目的

人間が全体像と判断点を素早く把握でき、AIが会話履歴やこのSkillなしでも同じHTMLを正本として解釈できる文書を作る。
情報を削って見かけ上短くするのではなく、検索、推論、統合、更新に必要な負担を減らす。

## 発火境界

このSkillを使う場面:

- HTML形式の技術文書を新規作成する。
- 既存HTML文書を改訂し、内容・構造・視覚体系を統一する。
- 文書の可読性、理解負債、正本性、AI可読性を監査する。
- 要求、決定、制約、運用手順、証拠の追跡関係を整理する。

このSkillを使わない場面:

- `@HLD.htmlを読んで実装する` など、既存文書を通常参照するだけの作業。
- 文書内容の単純な要約、質問回答、翻訳だけを行う作業。
- Webアプリ、ランディングページ、ダッシュボードなど、文書ではないUIを設計する作業。
- `.docx`、PDF、スライド、表計算を固有形式のまま作る作業。

通常参照でこのSkillが必要になる文書は不合格である。

## 最上位原則

1. **正本は一つにする。** 同じ要求、決定、制約、禁止事項を別の文章で重複させない。
2. **成果物は一つにする。** 一つの目的に対し、原則として一つの自己完結HTMLだけを生成する。
3. **読者を分けない。** 人間用とAI用の派生文書、隠し指示、重複要約を作らない。
4. **完全性を簡潔さより優先する。** 重要な条件を削らず、構造と段階的開示で負荷を下げる。
5. **意味を見た目だけに持たせない。** 色、位置、図、CSSクラスだけを根拠にしない。
6. **未確定を埋めない。** 推測した事実、数値、状態、日付、責任者を作らない。
7. **読解ツールへ依存させない。** 完成HTMLは、普通のファイル参照だけで解釈できなければならない。

文書分割は長さ、担当者、更新頻度、AIのコンテキスト節約を理由に行わない。
機密区分やアクセス制御が一つのファイルでは成立しない場合だけ競合を報告し、明示的な承認を得るまで分割しない。

## 実行前の完全性検証

このSkillを使用すると決めたら、文書作業を始める前にSkillディレクトリを検証する。

```text
python <skill-directory>/scripts/validate_skill_bundle.py <skill-directory>
```

期待結果は `ERROR 0` である。
検証スクリプトを実行できない場合、または一つでも欠落を検出した場合は、Skillの導入不完全として処理を停止する。
別のリポジトリ、作業対象の既存HTML、会話履歴から欠落資産を補完せず、利用者へ欠落した相対パスを報告する。

## 参照ファイルの選択

必要な参照だけを読む。

- 新規作成・大幅改訂: `references/naming-policy.html`、`references/single-document-model.html`、`references/human-ai-document-model.html`、`references/document-genre-matrix.html`、`references/hallmark-document-profile.html`、`references/quality-gates.html`
- 小規模改訂: 変更に関係する上記参照と `references/quality-gates.html`
- 監査: `references/single-document-model.html`、`references/human-ai-document-model.html`、`references/quality-gates.html`
- 研究根拠の確認・規範変更: `references/evidence-map.html`

参照HTMLのCSSは、内容理解に必要ないため読み込まない。

## 作業フロー

### 1. 指示と現状を確認する

- 適用されるプロジェクト指示を最終行まで読む。
- 既存文書、関連実装、用語、命名、リンク、作業ツリーを確認する。
- 既存文書の改訂では、内容を勝手に省略、統合、改名しない。
- 実装前承認が必要な環境では、目的、影響範囲、代替案、失敗モード、検証計画を提示して承認を得る。

### 2. 文書契約を決める

本文を書く前に、次を確定する。

- 目的: この文書によって何を判断、実装、運用できるようにするか。
- 読者: 初見の人、実装者、運用者、承認者、AIのうち誰が使うか。
- 対象範囲と対象外。
- 正本となる情報の種類。
- 文書状態、最終確認日、前提条件、未確定事項。
- 読了後に行う行動。

不明点が結論を変える場合は確認する。
不明でも安全に進められる場合は、仮定ではなく `未確定` として可視化する。

### 3. ファイル名を決める

`references/naming-policy.html` に従う。
利用者がファイル名を指定した場合は、標準名や安全性と衝突しない限り尊重する。

既知の標準名がなければ、原則は大文字ASCIIの `UPPER-KEBAB-CASE.html` とする。
一つだけなら `<TYPE>.html`、同種が複数なら `<TYPE>-<SUBJECT>.html` を使う。

### 4. 文書種別と読者の問いを選ぶ

`references/document-genre-matrix.html` から最も近い文書種別を選ぶ。
章立ては書き手の説明順ではなく、読者が判断または作業する順序にする。

見出しだけを読んで、目的、結論、理由、例外、次の行動が追えなければ構成を修正する。

### 5. 正本レコードを置く

要求、決定、制約、前提、禁止、推奨、未確定事項には、必要な範囲で安定IDを付ける。

推奨接頭辞:

- `REQ`: 要求
- `DEC`: 決定
- `CON`: 制約
- `ASM`: 前提
- `PRO`: 禁止
- `REC`: 推奨
- `OPN`: 未確定事項

各レコードでは、ID、種別、状態、規範強度、正本本文、適用範囲を人間にも見える形で示す。
理由、影響、例外、証拠は同じレコード内またはID参照で結び付ける。

概要や図は正本を言い換えない。
冒頭に主要決定を置く場合、その記述自体を正本レコードにする。

### 6. 本文を書く

- 一段落一論点とし、先頭文で段落の要点を示す。
- 結論、理由、条件、例外の順序を明確にする。
- 事実、決定、推論、推奨、未確定を混同しない。
- 同じ概念には同じ用語を使い、同義語の揺れを作らない。
- 主語、責任主体、対象、時点を省略して誤読させない。
- 「適切に」「必要に応じて」など、判定不能な語を単独で使わない。
- 説明文では観察、理由、結論の認知モードを切り替えてよいが、要件や手順へ物語的な緊張を持ち込まない。

箇条書きの項目数、文字数、段落数を認知科学の固定値として扱わない。
読者の課題と情報の相互依存性に応じてまとまりを決める。

### 7. 図表を選ぶ

- 3件以上の反復フィールドを比較する場合は表を検討する。
- 3件以上の要素間関係、依存、分岐、状態遷移が文章では追いにくい場合は図を検討する。
- 単純な一対一対応や一段階の操作は文章を優先する。
- 図表の近くに、読み取り方、結論、参照する正本IDを置く。
- 図だけに重要情報を持たせない。

### 8. Hallmark文書体系へ載せる

`references/hallmark-document-profile.html` と `assets/document-system/` を使用する。

基準構成は `Map / Diagram`、`modern-minimal`、適応版 `Cobalt`、デスクトップのサイドレール、モバイル索引、仕様表、段階列、インラインルールのフッターである。
これは視覚体系であり、文書種別固有の論理構造を上書きしない。

外部フォント、JavaScript、コマンドパレット、アニメーション、装飾目的の暗色帯は使わない。
色、書体、余白、罫線、角丸はトークンから参照する。

### 9. 一つの自己完結HTMLへ組み立てる

- `assets/document-system/DOCUMENT-TEMPLATE.html` を構造の出発点にする。
- `tokens.css` と `document.css` を一つの `<style>` に取り込み、外部CSSを成果物へコピーしない。
- 単純な構造図はインラインSVGにする。
- 外部スクリプト、外部フォント、Base64画像、実行時依存を原則として持ち込まない。
- CSS、SVGパス、クラス名に正本情報を持たせない。
- `details` 内へ必須、禁止、安全条件、主要結論を隠さない。

### 10. コールドリード監査を行う

Skill、会話履歴、作成者の意図を知らない読者が、HTML単体から次を特定できるか確認する。

- 文書の目的、対象範囲、対象外。
- 確定事項、未確定事項、必須事項、禁止事項。
- 正本として参照すべき記述。
- 例外、適用条件、競合時の優先関係。
- 実装または運用の次の行動。

一つでも推測が必要なら文書を修正する。

### 11. 機械監査を行う

作業ディレクトリを対象文書のあるディレクトリへ移し、次を実行する。

```text
python <skill-directory>/scripts/audit_document.py <DOCUMENT-NAME>.html
```

期待結果は `ERROR 0` である。
警告は無視せず、意図的な例外か修正対象かを確認する。
この監査は理解度を点数化せず、構文・構造・正本性の検査可能な兆候だけを扱う。

適用指示がHTML文法確認だけを求める場合は、その検証範囲を越えてブラウザやスクリーンショット検証を追加しない。

## 改訂規則

- 変更対象IDと、その参照元を先に特定する。
- 正本レコードを更新してから、図、表、索引、説明を追従させる。
- 旧記述を残す必要がある場合は `廃止` または `置換済み` とし、置換先IDを示す。
- 単なる改訂でファイル名を変更しない。
- 文書の意味が変わる改名は、リンク影響と移行方針を提示して承認を得る。

## 監査結果の形式

監査だけを依頼された場合は、次の順序で報告する。

1. 結論: 利用可、修正後利用可、利用不可。
2. 正本性: 重複、競合、欠落、状態不明。
3. 理解負債: 文脈、探索、推論、統合、更新、状態。
4. 人間・AI共通可読性: 通常参照だけで誤読し得る箇所。
5. 推奨修正: 優先度、対象ID、修正理由。

監査依頼だけではファイルを変更しない。

## 完了条件

- 成果物が目的に応じた名前の単一HTMLである。
- 人間用とAI用の内容が分かれていない。
- 正本となる記述が重複していない。
- 重要事項が可視本文に存在する。
- 通常の `@〇〇.html` 参照だけで作業を開始できる。
- HTML監査が `ERROR 0` である。
- 未確認事項と検証範囲を正確に報告している。
